<article class="guide-article">
  <slot />
</article>

<style>
  .guide-article {
    display: grid;
    gap: var(--space-5);
    color: var(--text-strong);
    font-size: 1rem;
    line-height: 1.7;
  }
</style>
