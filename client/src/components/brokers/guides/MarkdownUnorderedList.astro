<ul class="markdown-list">
  <slot />
</ul>

<style>
  .markdown-list {
    display: grid;
    gap: var(--space-2);
    margin: 0;
    padding-left: var(--space-6);
    color: var(--text-muted);
  }
</style>
